---
import { Image } from 'astro:assets';

import firstLogo from '~/assets/images/hb_logo.svg';

const logo = { src: firstLogo, alt: 'First Logo' };
---

<Image class="h-12 md:h-16 lg:h-20 2xl:h-16 w-auto ml-2" src={logo.src} format="svg" alt={logo.alt} loading="lazy" />
