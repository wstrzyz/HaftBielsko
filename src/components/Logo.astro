---
import { Image } from 'astro:assets';

import firstLogo from '~/assets/images/hb_logo.svg';

const logo = { src: firstLogo, alt: 'First Logo' };
---

<Image height={75} src={logo.src} format="svg" alt={logo.alt} loading="lazy" />
